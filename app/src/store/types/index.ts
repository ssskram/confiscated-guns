// user
export interface user {
  email: string;
  organization: string;
  name: string;
}

// message
export interface messsage {
  message: string;
}

// select
export type select = { value: string; label: string };
